{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Start by cloning the repository from Github: ``` # git clone https://github.com/hafsaChy/jsramverk-train-controller ```",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "style": "sass base.scss style.min.css --no-source-map",
    "style-min": "sass base.scss style.min.css  --no-source-map --style compressed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "normalize.css": "^8.0.1",
    "sass": "^1.66.1",
    "stylelint": "^15.10.3",
    "stylelint-config-sass-guidelines": "^10.0.0"
  }
}
